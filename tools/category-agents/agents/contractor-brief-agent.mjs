import {result} from '../helpers.mjs';
export const meta={id:'CONTRACTOR_BRIEF_AGENT',name:'Contractor Brief Agent',stage:'EXPERT_MODEL'};
export function run(ctx){const p=ctx.profile,ev=[],act=[];const req=p.contractorResponseRequired||[];for(const key of ['totalPrice','exclusions','extraCosts','leadTime','warranty'])if(!req.includes(key))act.push(`Добавить обязательное поле исполнителя: ${key}.`);if(req.length>=6)ev.push(`response contract=${req.length} fields`);const r=ctx.runtime.sdExpertAgent.run({serviceCode:p.serviceCode,city:'Москва',goal:'Проверка brief',scope:p.categoryId});const b=r.expert?.contractorBrief;if(!b?.title||!Array.isArray(b.scope)||!Array.isArray(b.responseRequirements))act.push('Runtime Contractor Brief не соответствует контракту.');else ev.push('runtime brief shape valid');return result(meta.id,act.length?'BLOCK':'PASS',act.length?'Contractor Brief недостаточно нормализован.':'Contractor Brief пригоден для запроса сопоставимых предложений.',ev,act)}