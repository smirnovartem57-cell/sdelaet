import {result,worst} from '../helpers.mjs';
export const meta={id:'CATEGORY_RELEASE_CONTROLLER',name:'Category Release Controller',stage:'RELEASE'};
export function run(ctx){const p=ctx.profile,prior=ctx.results||[],ev=[],act=[];const gate=worst(prior);if(gate==='BLOCK')return result(meta.id,'BLOCK','Категория не может двигаться дальше: есть блокирующие результаты агентов.',prior.filter(x=>x.status==='BLOCK').map(x=>x.agent),['Закрыть все BLOCK и повторить pipeline.']);if(p.status==='READY'){if(p.releaseGate?.status!=='PASS'||p.releaseGate?.productionDeploy!=='PASS')return result(meta.id,'BLOCK','READY не подтверждён release/production gate.',[],['Исправить releaseGate и production deploy.']);if(p.releaseGate?.manualReview!=='PASS'){act.push('Мигрировать legacy READY: явно зафиксировать releaseGate.manualReview=PASS.');ev.push('legacy READY accepted by existing releaseGate');return result(meta.id,'WARN','READY подтверждён legacy gate, но manualReview нужно сделать явным.',ev,act)}return result(meta.id,'PASS','READY подтверждён всеми обязательными gate.',[],[])}if(p.status==='TESTING')return result(meta.id,'PASS','Технический pipeline закрыт; следующий переход только после ручной проверки → READY.',[],['Провести manual user-facing review и production smoke перед READY.']);return result(meta.id,'PASS',`Текущий lifecycle=${p.status}; следующий переход определяется CATEGORY_LIFECYCLE.`,[],[])}