import fs from'node:fs';import vm from'node:vm';const c={window:{}};vm.createContext(c);for(const f of['category-engine.js','expert-agent.js','offer-parser.js','offer-followup.js','offer-normalizer.js'])vm.runInContext(fs.readFileSync(new URL('../assets/'+f,import.meta.url),'utf8'),c);const E=c.window.sdCategoryEngine,X=c.window.sdExpertAgent,P=c.window.sdOfferParser,F=c.window.sdOfferFollowup,N=c.window.sdOfferNormalizer;let n=0,z=0;function t(s,v){console.log(v?'PASS':'FAIL',++n,s);if(!v)z++}const cfg=JSON.parse(fs.readFileSync(new URL('../assets/category-profiles/mold-remediation.json',import.meta.url)));let a=E.analyze('Плесень и сырость в углу комнаты 4 м²');t('category',a.serviceCode==='MOLD_REMEDIATION');t('intake',a.questions.length<=5);let task={serviceCode:'MOLD_REMEDIATION',city:'Москва',goal:'Устранение плесени',scope:'4 м² в комнате повторно'};t('brief',X.run(task).expert.contractorBrief.responseRequirements.length>=12);let p=P.parse('Итого 65000 ₽. Площадь поражения 4 м². Диагностика, источник влаги — конденсация на мостике холода. Замеры влажности влагомером, проверка вентиляции и проверка протечки входят. Изоляция и защита зоны, удаление повреждённой штукатурки и плесени, механическая очистка и биоцидная система обработки, сушка, восстановление отделки и контрольный осмотр входят. Материалы включены. Срок 5 дней. Гарантия 1 год.');t('parse',(p.moldAffectedArea===4&&p.moldDiagnosticMethod&&p.moistureSource&&p.moistureMeasurementIncluded&&p.ventilationCheckIncluded&&p.leakCheckIncluded&&p.moldRemovalIncluded&&p.dryingIncluded&&p.followupControlIncluded));let full=N.normalize(p,task);t('comparable',full.comparable&&full.comparableTotalPrice===65000);t('from blocked',!N.normalize(P.parse('Удаление плесени от 5000 ₽.'),task).comparable);t('followup',F.build(task,{totalPrice:65000,materialsIncluded:true,leadTime:'5 дней',warranty:'2 года'}).items.length>0);t('research',cfg.research.result==='PASS');if(z)process.exit(1);console.log('MOLD_REMEDIATION QA PASS');