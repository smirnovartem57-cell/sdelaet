import fs from'node:fs';import vm from'node:vm';const c={window:{}};vm.createContext(c);vm.runInContext(fs.readFileSync(new URL('../assets/geo-classifier.js',import.meta.url),'utf8'),c);const G=c.window.sdGeoClassifier;let n=0,f=0;function t(s,v){console.log(v?'PASS':'FAIL',++n,s);if(!v)f++}let x=G.classify('Мытищи');t('Mytishchi region',x.regionId==='ru-mos'&&x.canonicalRegion==='Московская область');t('Mytishchi locality',x.canonicalLocality==='Мытищи'&&x.localityId==='ru-mos-mytishchi');t('Moscow',G.classify('Москва').regionId==='ru-mow');let e=G.classify('Тульская область, Тула');t('explicit region',e.regionId==='ru-tulskaya-oblast'&&e.canonicalLocality==='Тула'&&!e.needsConfirmation);let k=G.classify('Казань');t('Kazan directory locality',k.canonicalRegion==='Республика Татарстан'&&k.canonicalLocality==='Казань'&&!k.needsConfirmation);let u=G.classify('Новый Городок XYZ');t('unknown not guessed',u.canonicalRegion===''&&u.needsConfirmation&&u.canonicalLocality==='Новый Городок XYZ');let z=G.enrichTask({serviceCode:'WINDOW_REPAIR',city:'Химки'});t('task enriched',z.regionId==='ru-mos'&&z.locality==='Химки'&&z.geo.countryCode==='RU');t('empty safe',G.classify('').needsConfirmation);const html=fs.readFileSync(new URL('../create-task.html',import.meta.url),'utf8');t('create task wiring',html.includes('assets/geo-classifier.js')&&html.includes('regionId:geo.regionId')&&html.includes('localityId:geo.localityId'));if(f)process.exit(1);console.log('Geo classifier regression: '+n+'/'+n+' PASS');