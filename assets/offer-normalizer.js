(function(){
  function money(v){if(v==null||v==='')return null;var n=Number(String(v).replace(/[^0-9.,]/g,'').replace(',','.'));return isFinite(n)?Math.round(n):null}
  function bool(v){if(v===true||v==='yes'||v==='included')return true;if(v===false||v==='no'||v==='excluded')return false;return null}
  function normalize(raw){raw=raw||{};var o={candidateId:raw.candidateId||'',candidateName:raw.candidateName||'',rawResponse:raw.rawResponse||'',totalPrice:money(raw.totalPrice),workPrice:money(raw.workPrice),materialsPrice:money(raw.materialsPrice),materialsIncluded:bool(raw.materialsIncluded),worksIncluded:Array.isArray(raw.worksIncluded)?raw.worksIncluded:[],materialsSystem:raw.materialsSystem||'',finishIncluded:bool(raw.finishIncluded),demolitionIncluded:bool(raw.demolitionIncluded),wasteRemovalIncluded:bool(raw.wasteRemovalIncluded),electricsIncluded:bool(raw.electricsIncluded),warmFloorIncluded:bool(raw.warmFloorIncluded),measurement:raw.measurement||'',leadTime:raw.leadTime||'',warranty:raw.warranty||'',extraCosts:Array.isArray(raw.extraCosts)?raw.extraCosts:[],exclusions:Array.isArray(raw.exclusions)?raw.exclusions:[],unknowns:Array.isArray(raw.unknowns)?raw.unknowns:[]};
    var gaps=[];if(o.totalPrice==null)gaps.push('Не указана итоговая стоимость');if(o.materialsIncluded==null)gaps.push('Неясно, входят ли материалы');if(!o.worksIncluded.length)gaps.push('Не раскрыт состав работ');if(!o.leadTime)gaps.push('Не указан срок');if(!o.warranty)gaps.push('Не указана гарантия');
    var comparable=o.totalPrice;if(o.materialsIncluded===false&&o.materialsPrice!=null)comparable=(comparable||0)+o.materialsPrice;if(o.materialsIncluded===false&&o.materialsPrice==null)comparable=null;
    var completeness=100-gaps.length*15-o.unknowns.length*5-o.exclusions.length*3;completeness=Math.max(0,Math.min(100,completeness));
    var flags=[];if(o.materialsIncluded===false&&o.materialsPrice==null)flags.push('Цена без материалов — нельзя напрямую сравнивать с полной сметой');if(o.extraCosts.length)flags.push('Есть возможные дополнительные расходы');if(o.exclusions.length)flags.push('Есть исключения из стоимости');
    return Object.assign(o,{comparableTotalPrice:comparable,completenessScore:completeness,gaps:gaps,flags:flags,comparable:comparable!=null&&gaps.indexOf('Неясно, входят ли материалы')<0});
  }
  function rank(list){var n=(list||[]).map(normalize);var comparable=n.filter(function(x){return x.comparable&&x.comparableTotalPrice!=null});var min=comparable.length?Math.min.apply(null,comparable.map(function(x){return x.comparableTotalPrice})):null;n.forEach(function(x){var notes=[];if(x.comparable&&min!=null&&x.comparableTotalPrice===min)notes.push('Минимальная сопоставимая цена');if(x.completenessScore>=85)notes.push('Высокая полнота предложения');if(x.flags.length)notes=notes.concat(x.flags);x.explanation=notes.join('. ');});return n.sort(function(a,b){if(a.comparable!==b.comparable)return a.comparable?-1:1;if(a.comparableTotalPrice!=null&&b.comparableTotalPrice!=null&&a.comparableTotalPrice!==b.comparableTotalPrice)return a.comparableTotalPrice-b.comparableTotalPrice;return b.completenessScore-a.completenessScore})}
  window.sdOfferNormalizer={normalize:normalize,rank:rank};
})();