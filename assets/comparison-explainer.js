(function(){
  function uniq(a){return Array.from(new Set((a||[]).filter(Boolean)))}
  function money(v){return v==null?'':Number(v).toLocaleString('ru-RU').replace(/[\u00a0\u202f]/g,' ')+' ₽'}
  function explain(offers,task){offers=offers||[];task=task||{};var winner=offers.find(function(o){return o.recommendationStatus==='RECOMMENDED'})||null;var comparable=offers.filter(function(o){return o.comparable&&o.comparableTotalPrice!=null&&o.recommendationStatus!=='HIGH_RISK'});if(!winner&&comparable.length)winner=comparable.slice().sort(function(a,b){return a.comparableTotalPrice-b.comparableTotalPrice})[0];if(!winner)return{winner:null,reasons:[],checks:['Получить хотя бы одно сопоставимое предложение с понятной итоговой ценой, составом работ и без критичных рисков.'],summary:'Пока нельзя выбрать лучший вариант: предложения требуют уточнения или дополнительной проверки.'};var others=offers.filter(function(o){return o!==winner}),reasons=[];
    var cheaperHeadline=others.find(function(o){return o.totalPrice!=null&&winner.totalPrice!=null&&o.totalPrice<winner.totalPrice&&o.recommendationStatus!=='RECOMMENDED'});if(cheaperHeadline)reasons.push('Есть более низкая заявленная цена ('+money(cheaperHeadline.totalPrice)+'), но этот вариант не получил статус «Рекомендуем» из-за неполных данных, несопоставимой комплектации или риска.');
    var higher=others.filter(function(o){return o.comparable&&o.comparableTotalPrice>winner.comparableTotalPrice&&o.recommendationStatus!=='HIGH_RISK'}).sort(function(a,b){return a.comparableTotalPrice-b.comparableTotalPrice})[0];if(higher)reasons.push('Это минимальная сопоставимая стоимость: '+money(winner.comparableTotalPrice)+' против '+money(higher.comparableTotalPrice)+' у следующего безопасного сопоставимого варианта.');
    if(winner.materialsIncluded===true)reasons.push('Материалы включены в подтверждённую стоимость.');
    if((winner.missingCriticalWorks||[]).length===0)reasons.push('По критичным работам для этой задачи нет выявленных пропусков.');
    if(winner.completenessScore>=85)reasons.push('Предложение подробно раскрывает состав работ и условия.');
    reasons=uniq(reasons).slice(0,4);
    var checks=[];if(!winner.warranty)checks.push('Зафиксировать гарантию на работы до выбора исполнителя.');if(!winner.leadTime)checks.push('Подтвердить срок выполнения работ.');if((winner.extraCosts||[]).length)checks.push('Письменно уточнить, в каких случаях возникнут дополнительные расходы.');if((winner.exclusions||[]).length)checks.push('Проверить исключения из стоимости перед договорённостью.');if(/зим|круглогод/i.test(task.goal||''))checks.push('На замере подтвердить пригодность существующего остекления и примыканий для требуемого режима.');checks.push('Зафиксировать итоговый состав работ и цену после замера до начала работ.');checks=uniq(checks).slice(0,4);
    return{winner:winner,reasons:reasons,checks:checks,summary:'На текущих данных статус «Рекомендуем» получает '+(winner.candidateName||'это предложение')+' — по подтверждённой сопоставимой комплектации, а не только по минимальной заявленной цене.'};
  }
  window.sdComparisonExplainer={explain:explain};
})();